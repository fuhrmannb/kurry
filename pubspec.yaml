name: kurry
description: An open source cook platform

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.1.0-beta.2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  breakpoint: ^1.0.1
  responsive_scaffold: ^1.3.0
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  build_runner: ^1.10.0
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true
